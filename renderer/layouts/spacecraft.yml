title:
  extends:
    - safe-top
    - safe-left
  x: += .125in
  y: += .125in
  align: left
  font_size: 7

victory-points:
  extends:
    - title
  y: += .125in

stat-icon:
  height: 60
  width: 60

cost-icon:
  height: 0.1375in
  width: 0.1375in

construction:
  extends:
    - safe-top
    - safe-left
  x: += 1.25in
  y: += .125in
  width: 0.375in
  font_size: 10

command:
  extends:
    - construction
  x: += .5in

bombardment:
  extends:
    - safe-middle-horizontal
    - safe-top
  y: += 1.25in
  x: += .25in

accuracy:
  extends:
    - safe-middle-horizontal
    - safe-top
  y: += 1.25in
  x: -= .5in

armor:
  extends:
    - safe-middle-horizontal
    - safe-top
  y: += .75in
  x: -= 50

regen:
  extends:
    - safe-middle-horizontal
    - safe-top
  y: += .75in
  x: += 40

delta-vee:
  extends:
    - safe-middle-horizontal
    - safe-top
  y: += 2in
  x: -= 30

abilities:
  extends:
    - safe-width
    - safe-bottom
    - safe-left
    - debug
  width: -= .25in
  x: += .125in
  y: -= 1in

abilities:
  extends:
    - safe-left
    - safe-bottom
    - safe-width
    - abilities-font
  width: -= .25in
  height: .75in
  x: += .125in
  y: -= .875in
